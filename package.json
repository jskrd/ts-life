{
	"name": "ts-life",
	"module": "src/index.ts",
	"devDependencies": {
		"@biomejs/biome": "1.6.4",
		"@types/bun": "latest",
		"@vercel/analytics": "^1.2.2"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"scripts": {
		"bundle": "bun build ./src/index.ts --outfile=public/app.js",
		"dev": "bun run bundle --watch",
		"prod": "bun run bundle --minify",
		"format": "biome format . --write",
		"lint": "biome lint ."
	},
	"type": "module"
}
